name: movies_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  #Flutter widgets implementing the current IOS design language.
  cupertino_icons: ^1.0.2
  #Being able to compare objects in Dart using `==` operator as well as `hashCode`.
  equatable: ^2.0.5
  #HTTP networking package
  dio: ^5.4.0
  #Functional programming in Dart
  dartz: ^0.10.1
  #State management package
  flutter_bloc: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true

